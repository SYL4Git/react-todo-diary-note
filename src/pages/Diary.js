import { useCallback, useEffect, useRef, useReducer, useMemo } from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

const Diary = () => {
	<section>
		<div className='pgDescription'>간단한 일기를 쓸 수 있는 공간입니다.</div>
	</section>;
};
export default Diary;
